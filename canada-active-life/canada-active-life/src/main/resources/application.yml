server:
  port: 8080

spring:
  application:
  datasource:
    url: jdbc:postgresql://localhost:5432/canadaActiveLife
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true