server:
  port: 8080

spring:
  application:
    name: ems
  datasource:
    url: jdbc:postgresql://localhost:5432/ems-app
    username: postgres
    password: postgres
  jpa:
    show-sql: true
  activemq:
    broker-url: tcp://localhost:61616
